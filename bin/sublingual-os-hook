#!/bin/bash

# Run a named hook, like post-update (available in ~/.config/sublingual-os/hooks/post-update).

set -e

if (( $# < 1 )); then
  echo "Usage: sublingual-os-hook [name] [args...]"
  exit 1
fi

HOOK=$1
HOOK_PATH="$HOME/.config/sublingual-os/hooks/$1"
shift

if [[ -f $HOOK_PATH ]]; then
  bash "$HOOK_PATH" "$@"
fi
