#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/sublingual-os/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  sublingual-os-theme-set "$(cat $THEME_NAME_PATH)"
fi
