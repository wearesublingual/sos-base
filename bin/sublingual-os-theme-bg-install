#!/bin/bash

CURRENT_THEME_NAME=$(cat "$HOME/.config/sublingual-os/current/theme.name")
THEME_USER_BACKGROUNDS="$HOME/.config/sublingual-os/backgrounds/$CURRENT_THEME_NAME"

mkdir -p "$THEME_USER_BACKGROUNDS"
nautilus "$THEME_USER_BACKGROUNDS"
