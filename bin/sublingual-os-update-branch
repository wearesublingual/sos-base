#!/bin/bash

set -e

if (($# == 0)); then
  echo "Usage: sublingual-os-update-branch [master|dev]"
  exit 1
fi

branch="$1"

# Snapshot before switching branch
sublingual-os-snapshot create || (( $? == 127 ))

if ! git -C "$SUBLINGUAL_OS_PATH" diff --quiet || ! git -C "$SUBLINGUAL_OS_PATH" diff --cached --quiet; then
  stashed=true
  git -C "$SUBLINGUAL_OS_PATH" stash push -u -m "Autostash before switching to $branch"
else
  stashed=false
fi

# Switch branches
git -C "$SUBLINGUAL_OS_PATH" switch "$branch"

# Reapply stash if we made one
if [[ $stashed == "true" ]]; then
  if ! git -C "$SUBLINGUAL_OS_PATH" stash pop; then
    echo "⚠️ Conflicts when applying stash — stash kept"
  fi
fi

# Update the system from the new branch
sublingual-os-update-perform
