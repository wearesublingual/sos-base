#!/bin/bash

# Launch the Sublingual OS bluetooth controls TUI (provided by bluetui).
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec sublingual-os-launch-or-focus-tui bluetui
